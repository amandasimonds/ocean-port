import styled from "styled-components";
import { Card } from "reactstrap";

export const SharkWrapper = styled(Card)`
    CardHeader {
        font-size: 22px;
        font-weight: 700;
    }
    p1 {
        float: left;
    }

    p2 {
        font-size: 18px;
        font-weight: 500;
    }
`